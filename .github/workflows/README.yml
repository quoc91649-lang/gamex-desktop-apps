# This file is auto-generated by GameX platform
# Do not edit manually - workflows are created per-app

name: Build Template (Reference Only)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Workflows are auto-generated per app by GameX platform"
