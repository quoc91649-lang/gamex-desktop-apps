name: Build Desktop App - anh ten la bang

on:
  push:
    paths:
      - 'apps/anh-ten-la-bang-1778127282007/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-exe:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        working-directory: apps/anh-ten-la-bang-1778127282007
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          if (Test-Path "requirements.txt") { pip install -r requirements.txt }
        shell: pwsh

      - name: Build .exe with PyInstaller
        working-directory: apps/anh-ten-la-bang-1778127282007
        run: pyinstaller --onefile --windowed --name "anh-ten-la-bang" main.py
        shell: pwsh

      - name: Get version
        id: version
        run: |
          $runNumber = "${{ github.run_number }}"
          "version=1.0.$runNumber" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
        shell: pwsh

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "anh-ten-la-bang-v${{ steps.version.outputs.version }}"
          name: "anh ten la bang v${{ steps.version.outputs.version }}"
          files: apps/anh-ten-la-bang-1778127282007/dist/anh-ten-la-bang.exe
          body: |
            ## anh ten la bang
            Auto-built from source by GameX platform.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update build status
        if: always()
        run: |
          $jobStatus = "${{ job.status }}"
          $status = if ($jobStatus -eq "success") { "success" } else { "failed" }
          $body = @{ app_id = "apps/anh-ten-la-bang-1778127282007"; status = $status; run_id = "${{ github.run_id }}" } | ConvertTo-Json
          $webhookUrl = "${{ vars.GAMEX_WEBHOOK_URL }}"
          try { Invoke-RestMethod -Uri "$webhookUrl/api/desktop-build-webhook" -Method POST -Body $body -ContentType "application/json" } catch { Write-Host "Webhook failed: $_" }
        shell: pwsh